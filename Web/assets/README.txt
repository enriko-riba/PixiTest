The premultiply_alpha.bat is using imagemagick to add alpha / premultiply alpha with color channels.
ImageMagick needs to be in the path environment variable, during install leave the 'Update system path' setting checked. 
ImageMagick can be downloaded from: https://www.imagemagick.org/script/binary-releases.php
